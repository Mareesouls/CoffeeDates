using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Day1Manager : MonoBehaviour
{
    /*Use this script as a template to guide you through the next few days,
    What we need in the level manager is to have it store is the amount of customer in a day and what datable customer would appear
    At first this script might seem useless but it is vital to have a basic level manager if we want to reload this level or expand for future updates
     */


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
