using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CID_Espresso : MonoBehaviour
{
    string coffeeName = "Espresso";
    public int coffeeID = 1;
    //Drink type
    bool coffee = true;
    bool tea = false;
    //Drink properties
    bool sweet = false;
    bool bitter = true;
    bool forKids = false;


    /* Personally not a coffee drinker, still researching on different flavours of drinks. If something is missing just added it in to the list of bools
    bool  = ;
    bool  = ;
    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
